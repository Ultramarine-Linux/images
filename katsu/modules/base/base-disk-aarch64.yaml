builder: dnf
distro: Ultramarine Linux Cheetah

# users:
#   - username: ultramarine
#     # plaintext password: ultramarine
#     password: "$y$j9T$6/DebcxXazPrtBYnNXtEM.$yaUJHww5Mo1L8xNJ9IDJ.bvKOrIJxAG9PGQKWioBMx3"
#     groups:
#       - wheel

import:
  - base.yaml
  - base-disk.yaml

dnf:
  releasever: rawhide
  exec: dnf5
  arch: aarch64
  packages:
    - ultramarine-release-identity-basic
scripts:
  post:
    - id: grub
      name: Set up GRUB
      file: grub-setup.sh
    - id: multi-user
      priority: 60
      name: Set default target to multi-user
      file: default-target.sh
    - id: u-boot
      name: Install RPi and Pinebook Pro U-Boot
      file: uboot.sh

disk:
  size: 10GB
  partitions:
    - label: EFI
      type: esp
      size: 512MB
      filesystem: efi
      mountpoint: /boot/efi

    - label: boot
      type: xbootldr
      size: 1GB
      filesystem: ext4
      mountpoint: /boot

    - label: root
      flags:
        - grow-fs
      type: root
      # size: 2.5MB
      filesystem: ext4
      mountpoint: /
