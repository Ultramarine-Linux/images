builder: dnf4

dnf:
  repodir: repodir/
  releasever: 44
  exec: dnf5
  options:
    - --setopt=cachedir=/var/cache/dnf
    - --setopt=keepcache=True
    - --setopt=tsflags=nodocs
    - --setopt=max_parallel_downloads=20
    - --setopt=install_weak_deps=True
  exclude:
    - fedora-release*
    - generic-release*
    - kernel*
  packages:
    # - uutils-coreutils-replace
    - filesystem
    - setup
    - glibc
    - glibc-common
    - dnf
    - git
    # - mkpasswd # maybe not needed soon
    - "@core"
    - "@hardware-support"
    - rpm
    - libgomp
    - ultramarine-release-identity-container
    - fedora-repos
