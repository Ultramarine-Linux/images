builder: dnf

import:
  - ../base/base.yaml
  - ../live/live.yaml
  - ../base/desktop.yaml
  - ../live/readymade.yaml
  - budgie.yaml

distro: Ultramarine Budgie 45

out_file: ultramarine-budgie-cheeetah-live.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-Budgie-Live

scripts:
  post:
    - id: budgie-live
      name: Set up Budgie Live
      file: ./budgie-live.sh

dnf:
  releasever: 45
  exec: dnf5
