builder: dnf4

import:
  - ../base/base.yaml
  - ../base/desktop.yaml
  - ../live/live.yaml
  - ../live/anaconda.yaml
  - gnome.yaml

distro: Ultramarine GNOME 44

out_file: ultramarine-gnome-44-live-anaconda.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-Gn-Live-Anaconda

scripts:
  post:
    - id: gnome-live
      name: Set up GNOME Live
      file: gnome-live.sh
    - id: gnome-live-anaconda
      name: Set up GNOME Live (for Anaconda)
      file: gnome-live-anaconda.sh

dnf:
  releasever: 44
  exec: dnf5
