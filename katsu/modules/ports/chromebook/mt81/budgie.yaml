builder: dnf

import:
  - mt8183.yaml
  - ../../../budgie/budgie.yaml
  - ../../../budgie/base-disk-aarch64.yaml

distro: Ultramarine Budgie Cheetah (mt8183 Chromebook)

out_file: ultramarine-cb-mt8183-budgie-cheetah.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
