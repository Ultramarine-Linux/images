builder: dnf

import:
  - mt8183.yaml
  - ../../../gnome/gnome.yaml
  - ../../../gnome/base-disk-aarch64.yaml

distro: Ultramarine GNOME Cheetah (mt8183 Chromebook)

out_file: ultramarine-cb-mt8183-gnome-cheetah.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
