builder: dnf

import:
  - mt8183.yaml
  - ../../../xfce/xfce.yaml
  - ../../../xfce/base-disk-aarch64.yaml

distro: Ultramarine Xfce Cheetah (mt8183 Chromebook)

out_file: ultramarine-cb-mt8183-xfce-cheetah.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
