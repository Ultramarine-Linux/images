builder: dnf

import:
  - ../chromebook.yaml
  - ../../../gnome/gnome-live.yaml
  - ../../../gnome/base-disk-x86_64.yaml

distro: Ultramarine GNOME Cheetah (Chromebook Edition)

out_file: ultramarine-cb-gnome-cheetah.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
