builder: dnf

import:
  - ../chromebook.yaml
  - ../../../xfce/xfce-live.yaml
  - ../../../xfce/base-disk-x86_64.yaml

distro: Ultramarine Xfce 45 (Chromebook Edition)

out_file: ultramarine-cbe-xfce-45.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: 45
  exec: dnf5
