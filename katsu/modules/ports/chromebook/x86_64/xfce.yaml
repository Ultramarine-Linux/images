builder: dnf

import:
  - ../chromebook.yaml
  - ../../../xfce/xfce-live.yaml
  - ../../../xfce/base-disk-x86_64.yaml

distro: Ultramarine Xfce Cheetah (Chromebook Edition)

out_file: ultramarine-cbe-xfce-cheetah.img

bootloader: grub

kernel_cmdline: "quiet rhgb"

dnf:
  releasever: rawhide
  exec: dnf5
