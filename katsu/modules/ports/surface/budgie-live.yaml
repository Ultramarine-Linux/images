builder: dnf

import:
  - ../../base/base.yaml
  - ../../live/anaconda.yaml
  - ../../live/live.yaml
  - ../../budgie/budgie.yaml
  - surface.yaml

distro: Ultramarine Budgie Cheetah

out_file: ultramarine-surface-budgie-cheetah.iso

bootloader: grub

kernel_cmdline: "quiet rhgb"

iso:
  volume_id: UM-budgie-Live

scripts:
  post:
    - id: budgie-live
      name: Set up Budgie Live
      file: ../../budgie/./budgie-live.sh

dnf:
  releasever: rawhide
  exec: dnf5
